import 'package:flutter/material.dart';

class RegisterCaracter extends StatefulWidget {
  const RegisterCaracter({ Key? key }) : super(key: key);

  @override
  State<RegisterCaracter> createState() => _RegisterCaracterState();
}

class _RegisterCaracterState extends State<RegisterCaracter> {
  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;
    return Scaffold(
      appBar: AppBar(

      ),
      body: Column(
        children: [
        ],
      ),
    );

  }
}